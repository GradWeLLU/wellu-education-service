spring.application.name=wellu-education-service
    server.port=8083

# ========================
# DATABASE
# ========================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://education-db:5432/education}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:education}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:password}

# Hikari (connection pool)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2

# ========================
# JPA / HIBERNATE
# ========================
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.default_schema=public

# ========================
# FLYWAY
# ========================
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# ========================
# AI SERVICE
# ========================
ai-service.base-url=${AI_SERVICE_BASE_URL:http://host.docker.internal:8000}

# ========================
# SCHEDULING
# ========================
scheduling.enabled=true

# ========================
# JWT
# ========================
jwt.secret=${JWT_SECRET}
jwt.expiration=3600000


